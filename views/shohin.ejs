<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <title>商品管理システム</title>
</head>
<body>
  <h2>商品在庫一覧</h2>
  <table border="1" style="border-collapse: collapse; width: 600px;">
    <tr><th>ID</th><th>商品名</th><th>価格</th><th>在庫数</th><th>操作</th></tr>
    <% let j = 0 %>
    <% for( let item of data ) { %>
      <tr>
        <td style="text-align:center;"><%= item.id %></td>
        <td><a href="/shohin/<%= j %>"><%= item.name %></a></td>
        <td><%= item.price %> 円</td>
        <td style="<%= item.stock < 10 ? 'color:red; font-weight:bold;' : '' %>">
          <%= item.stock %> 個
        </td>
        <td style="text-align:center;">
          <form action="/shohin/delete/<%= j %>" method="POST" style="margin:0;">
            <input type="submit" value="削除" onclick="return confirm('削除しますか？');">
          </form>
        </td>
      </tr>
      <% j += 1 %>
    <% } %>
  </table>

  <h3>商品追加</h3>
  <form action="/shohin/create" method="POST" style="background:#e0f7fa; padding:10px; width:580px;">
    商品名: <input type="text" name="name" required>
    価格: <input type="number" name="price" value="0" style="width:60px;">円
    在庫: <input type="number" name="stock" value="0" style="width:50px;">個
    <input type="submit" value="追加">
  </form>
</body>
</html>